{
    "mcpServers": {
        "playwright": {
            "command": "npx",
            "args": ["-y", "@playwright/mcp"],
            "disabled": false,
            "env": {
                "PLAYWRIGHT_BROWSERS_PATH": "${project_root}/node_modules/.playwright",
                "DEBUG": "pw:api"
            }
        }
    }
}

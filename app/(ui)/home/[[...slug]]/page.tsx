'use client';

// React Router DOM은 클라이언트 사이드 전용이므로 'use client' 필요
// Next.js의 catch-all 라우트 ([...slug])로 모든 경로를 React Router DOM으로 위임

export default function Home2Page() {
    return null; // 실제 렌더링은 ClientRouter에서 처리
}
